import * as AUTH_APIS from "./auth"
import * as USER_APIS from "./user"
import * as MESSAGE_APIS from "./message"

export const API = {
  AUTH: AUTH_APIS,
  USER: USER_APIS,
  MESSAGE: MESSAGE_APIS,
}